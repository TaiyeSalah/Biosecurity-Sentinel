# ═══════════════════════════════════════════════════════════════
# Biosecurity Sentinel — Environment Configuration
# Copy to .env and fill in your values. Never commit .env to git.
# ═══════════════════════════════════════════════════════════════

# Application
SENTINEL_APP_NAME="Biosecurity Sentinel"
SENTINEL_DEBUG=false
SENTINEL_LOG_LEVEL=INFO

# API
SENTINEL_API_HOST=0.0.0.0
SENTINEL_API_PORT=8001

# Model weights (leave blank until trained)
SENTINEL_MODEL_PATH=

# Inference
SENTINEL_N_MC_SAMPLES=30

# Risk thresholds (0.0–1.0)
SENTINEL_TOX_CRITICAL_THRESHOLD=0.85
SENTINEL_TOX_HIGH_THRESHOLD=0.65
SENTINEL_TOX_MODERATE_THRESHOLD=0.40
SENTINEL_DUAL_USE_HALT_THRESHOLD=0.85

# Database (SQLite default — switch to PostgreSQL for production)
SENTINEL_DATABASE_URL=sqlite+aiosqlite:///./biosecurity_audit.db

# Audit
SENTINEL_AUDIT_LOG_PATH=./logs/audit.jsonl
SENTINEL_REQUIRE_REQUESTER_ID=false
